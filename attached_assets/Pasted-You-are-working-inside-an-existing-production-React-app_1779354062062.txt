You are working inside an existing production React app:
https://github.com/Cliff-e/deriv-edge-clean

THIS IS A PRODUCTION CODEBASE. ASSUME EVERYTHING OUTSIDE THIS TASK ALREADY WORKS.

========================
STRICT SCOPE (HARD LIMIT)
========================
You are ONLY allowed to work on authentication features:

ALLOWED:
- Deriv OAuth login (PKCE flow)
- OAuth callback handling
- state + code_verifier handling
- token exchange integration (backend API only, do NOT implement backend)
- legacy login compatibility ONLY if already present in auth flow

ONLY THESE PATHS MAY BE MODIFIED OR CREATED:
- src/auth/**
- src/pages/Login*
- src/pages/Callback*
- src/components/Login*

========================
ABSOLUTE FORBIDDEN ACTIONS
========================
DO NOT:
- refactor any existing architecture
- rename files or folders outside allowed paths
- modify routing unless directly inside Login/Callback pages
- touch dashboard, business logic, API services, or state management outside auth
- “improve”, “optimize”, or “clean up” unrelated code
- reorganize project structure
- change styling system unless required for login UI only
- introduce new auth systems beyond Deriv OAuth + existing legacy login

========================
BEHAVIOR RULES
========================
1. Treat existing code as production and stable.
2. Prefer minimal diffs over improvements.
3. If something is missing, ASK before assuming.
4. Do NOT rewrite files unless required for auth flow.
5. Do NOT move code between folders.
6. Do NOT change unrelated imports.
7. Do NOT generalize or “improve architecture”.

========================
TASK
========================
Implement ONLY:
- Deriv OAuth2 PKCE login flow
- secure state + verifier generation/storage
- callback page handling
- backend token exchange request (frontend only calls API)
- coexistence with legacy login system (do not modify legacy logic unless required for integration)

========================
OUTPUT FORMAT
========================
- Show ONLY new or modified files
- Brief explanation per file (1–3 lines max)
- NO refactor suggestions
- NO architecture commentary
- NO extra improvements outside scope